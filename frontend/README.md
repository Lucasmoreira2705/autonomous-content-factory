# Frontend

Reserved for the Next.js + React + TypeScript dashboard. It is intentionally not implemented in Phase 1A because the project specification requires the backend pipeline to work before expanding to the visual control plane.
